# WHOOP 5 — المعادلات الكاملة (المحلية بلا سحابة)

مرجع لكل معادلة: **الموجودة** (مفكوكة ومتحقّقة على بياناتك) و**الناقصة** (نعرف صيغتها، ينقصها مدخل واحد).
الرموز: `HR`=نبض(bpm) · `W`=وزن(kg) · `A`=عمر · `rest`=نبض الراحة · `max`=أقصى نبض · `dt`=فترة العيّنة(s).

---

# القسم أ — معادلات موجودة ومتحقّقة ✅

## 1. HR-Reserve (Karvonen) — أساس كل شي
```
HRR = (HR − rest) / (max − rest)          مقصوصة [0,1]
تحت 0.3  →  تُعامل صفر (أرضية الراحة)
```
[JAVA qh0/d]

## 2. Strain (WHOOP، مقياس 0–21)
```
مساهمة كل عيّنة:   s(HRR, dt) = 16 / (e^((1−HRR)/0.15) + 1) · dt
raw = Σ s / 691200                          (مقصوص [0,1])
Strain(0–21) = جدول تحويل لوغاريتمي(raw)     [211 نقطة]
```
[JAVA qh0/o, qh0/d] — سيغمويد لوجستي يتراكم عبر اليوم.

## 3. السعرات (Keytel 2005 + BMR)
**أثناء المجهود** (ك*جول/دقيقة، ثم ÷ للتحويل لـkcal/s):
```
رجال:  EE = −55.0969 + 0.6309·HR + 0.1988·W + 0.2017·A
نساء:  EE = −20.4022 + 0.4472·HR − 0.1263·W + 0.0740·A
```
**أثناء الراحة** (BMR، عندما HRR < 0.3):
```
RMR = restAlpha + restWeight·W + restHeight·height − restAge·A      (kcal/يوم ÷ 86400)
```
المجموع: `kJ = Σ (dt · معدّل_الحرق) × 4.184`  [JAVA qh0/b]

## 4. مناطق النبض (Zones، دقائق)
```
x = HRR ثم:  Z1≥0.5 · Z2≥0.6 · Z3≥0.7 · Z4≥0.8 · Z5≥0.9
```
[PUB Karvonen]

## 5. اللياقة
```
HRmax (Tanaka 2001):   208 − 0.7·A
VO2max (Uth 2004):     15.3 · HRmax / rest
```

## 6. HRV (RMSSD) — من موجة v26 PPG
```
RMSSD = √( متوسط( (RR[i+1] − RR[i])² ) )        (بعد رفض الشاذّ)
المعدّل (المرساة):  HR = fs·60 / lag*    حيث lag* = ذروة autocorrelation
الجودة GOOD/COARSE/POOR حسب |HR_نبضات − HR_مرساة|
```
[NOOP corr +0.907] — مُصلَح: المرساة تمنع العدّ المزدوج على 24Hz.

## 7. IMU (حركة/خطوات)
```
تسارع(g) = raw · 8/32768        (±8g، WHOOP5)
جايرو(dps) = raw · 2000/32768   (±2000dps)
|a| = √(ax²+ay²+az²)            (~1.0 راحة)
حركة% = نسبة العيّنات |a−baseline| > 0.05g
خطوات = ذرى |a| المُزال منها الجاذبية > 0.08g، فترة صمّاء 0.3s
```

## 8. النوم (Cole-Kripke actigraphy)
```
D = P · Σ( wₖ · activity[epoch+k] )       wₖ = [106,54,58,76,230,74,67]
نوم إذا D < 1، صحو إذا D ≥ 1؛ ثم z-scoring لتصنيف Deep/Light/REM/Wake
```
[algorithm: GenieMax]

---

# القسم ب — معادلات الأشياء الناقصة ⏳ (الصيغة معروفة، ينقص مدخل)

## 9. SpO2 (نسبة الأكسجين) — ratio-of-ratios
**الصيغة كاملة ومعروفة:**
```
لكل طول موجي:  AC = سعة النبض (peak-to-peak)  ·  DC = الأساس (المتوسط)
النسبة:        R = (AC_red / DC_red) / (AC_ir / DC_ir)
SpO2 = a·R² + b·R + c
    منحنى Maxim (MAX30102):  −45.060·R² + 30.354·R + 94.845
    تقريب خطّي شائع:          110 − 25·R
```
**الرقاقة عندك: MAX86176** (من سجلّات فيرموير ملفك) — صيغة FIFO موثّقة.

**الناقص (مدخلان، مو معادلة):**
1. **قناتا red/IR فعليّتان** → تُشغَّلان **بالنوم فقط**؛ يبيلها **offload بعد نوم**.
2. **مرجع للمعايرة** → قيمتان مختلفتان من مقياس إصبعي (مثلاً راحة 96% + حبس‑نفَس 91%) لحساب `a,b,c` وتمييز red من IR.

## 10. Recovery / Readiness — الصيغة **جاهزة في الكود**
```
z_hrv   = (HRV_اليوم − μ_HRV) / σ_HRV
z_rhr   = (RHR_اليوم − μ_RHR) / σ_RHR
z_rr    = (RR_اليوم  − μ_RR)  / σ_RR
z_sleep = انحراف نوم اليوم

Recovery = 100 · Φ( 0.55·z_hrv − 0.20·z_rhr − 0.10·z_rr + 0.15·z_sleep )
Readiness = 0.5·Recovery + 0.3·sleep_score + 0.2·TSB_norm − عقوبة_مرض
```
`Φ` = دالة التوزيع الطبيعي التراكمي. **الناقص:** `μ,σ` = خطوط أساس **7 أيام** (WHOOP نفسه يطلبها).

## 11. skin_temp → °C — الصيغة معروفة
```
مقاومة الثرمستور:  R_T = R_ref · raw/(full − raw)     (من دائرة القسمة)
Steinhart–Hart:     1/T(K) = a + b·ln(R_T) + c·(ln R_T)³
WHOOP يعرض الانحراف:  ΔT = T_اليوم − T_أساس_شخصي
```
عندك raw خام (3140–3508). **الناقص:** ثوابت `a,b,c` للثرمستور (من مرجع حرارة، أو من تصدير WHOOP).

## 12. Respiration Rate (تنفّس/دقيقة) — من تعديل PPG/الحركة
```
BR = التردد المهيمن (0.1–0.5 Hz) لظرف موجة PPG أو مغزل الصدر بالـIMU
    (RIIV: تعديل السعة التنفّسي على PPG)
```
عندك `respiration_raw` خام (15269–16687). **الناقص:** معايرة الظرف → br/min.

## 13. v20 (2115 سجل) — **لا معادلة بعد**
سجل تاريخي غير مُخطّط (index/unix/subsec فقط). يبيله ground truth (تصدير WHOOP) لمطابقة الحقول — **قاعدة المشروع تمنع اختراع offsets**.

---

## الخلاصة
| # | القراءة | الحالة |
|---|---|---|
| 1–8 | HR·Strain·سعرات·Zones·لياقة·HRV·IMU·نوم | ✅ معادلة + متحقّقة |
| 9 | SpO2 | ⏳ معادلة جاهزة · يبي offload‑نوم + مرجع |
| 10 | Recovery | ⏳ معادلة جاهزة · يبي 7 أيام أساس |
| 11 | skin_temp°C | ⏳ معادلة جاهزة · يبي ثوابت ثرمستور |
| 12 | تنفّس | ⏳ معادلة جاهزة · يبي معايرة |
| 13 | v20 | ❌ يبي ground truth |
